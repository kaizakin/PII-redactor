grpcio==1.83.0
grpcio-tools==1.83.0
presidio-analyzer==2.2.364
spacy==3.8.15
pytesseract==0.3.13
Pillow==12.3.0

# spaCy's small English pipeline. Not on PyPI proper — installed as a
# direct wheel URL, which pip handles like any other dependency. Swapping
# this for en_core_web_lg (higher accuracy, larger download) or a
# transformer-based pipeline is the only change needed to upgrade the NLP
# model; nothing else in this service or the Go engine changes. But
# en_core_web_lg is NOT a drop-in accuracy win — see the comment on
# DEFAULT_MODEL in analyzer.py for a real regression it introduced.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
